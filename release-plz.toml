[workspace]
# This crate is not published to crates.io yet. Use git tags as the release
# source of truth and skip cargo publishing while still updating Cargo metadata,
# CHANGELOG.md, git tags, and GitHub Releases.
git_only = true
release_always = false
changelog_update = true
git_tag_enable = false
git_release_enable = false

[[package]]
name = "leptos-mcp-server"
# Keep the package override in git-only mode too. Package-level release settings
# override workspace defaults, and this crate is intentionally not on crates.io.
git_only = true
changelog_path = "CHANGELOG.md"
git_tag_name = "v{{ version }}"
git_tag_enable = true
git_release_enable = true
